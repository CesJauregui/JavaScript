// Módulos externos
const os = require("os");
console.log(os.platform()); // Core del sistema operativo
console.log(os.arch()); // Arquitectura del sistema
console.log(os.totalmem()); // memoria total
console.log(os.freemem()); // memoria libre
